# Copy to .env and fill in real values. .env is gitignored — never commit it.
# Canvas: mint at csd509j.instructure.com → Account → Settings → New Access Token.
# (Pre-hack token is quarantined in 99_ARCHIVE and considered exposed — revoke it there first.)
CANVAS_API_TOKEN=your_canvas_token_here
CANVAS_API_URL=https://csd509j.instructure.com
# Gemini: used by tools/swarm_evaluator.py + canvas_exemplar_portfolio.py
GEMINI_API_KEY=your_gemini_key_here
