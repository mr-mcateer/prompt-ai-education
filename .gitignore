# Security: Never commit secrets
.env
.env.*

# OS artifacts
.DS_Store
**/.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Claude Code local settings (may contain token references)
.claude/settings.local.json

# Planning state (ephemeral, not for version control)
.planning/state.md

# Temporary files
/tmp/
*.tmp

# but DO version the example — it documents required secret names
!.env.example

# student PII -- never commit (exposure audit 2026-09-09)
*grades_export*.csv
draft_grades*.csv
gradebook_snapshot.json
detailed_audit.json
score_anomaly_report.json
**/canvas-agent-grader/exports/
synergy_*.json
